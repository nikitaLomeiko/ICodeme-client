"use client";

import { Document } from "@/entities/document";
import { Button } from "@/shared/ui/kit";
import { Layout } from "@/widgets/layout";
import { useRouter } from "next/navigation";
import { FaArrowLeft } from "react-icons/fa";

const markdown = `# Заголовок H1

## Заголовок H2

### Заголовок H3

#### Заголовок H4

Обычный параграф текста. Здесь можно написать любой текст, который будет отображаться как обычный абзац. Поддерживается **жирный текст** и *курсив*, а также ***жирный курсив***.

## Текстовое форматирование

Это **жирный текст**, а это *курсив*. Можно делать ~~зачеркнутый текст~~ и \`инлайн код\`. Также поддерживаются **вложенные** форматирования внутри кода.

## Списки

### Маркированный список:
- Первый пункт
- Второй пункт
  - Вложенный пункт
  - Еще один вложенный
- Третий пункт

### Нумерованный список:
1. Первый шаг
2. Второй шаг
   1. Подшаг 1
   2. Подшаг 2
3. Третий шаг

### Чеклисты (GFM):
- [x] Выполненная задача
- [ ] Невыполненная задача
- [ ] Еще одна задача

## Блоки кода

### JavaScript с подсветкой:
\`\`\`javascript
// Комментарий
const greeting = "Hello, World!";
const numbers = [1, 2, 3, 4, 5];

numbers.forEach((num) => {
  console.log(\`Number: \${num}\`);
});

function calculateSum(a, b) {
  return a + b;
}

// Стрелочная функция
const multiply = (x, y) => x * y;
\`\`\`

### TypeScript:
\`\`\`typescript
interface User {
  id: number;
  name: string;
  email: string;
}

const getUser = (id: number): Promise<User> => {
  return fetch(\`/api/users/\${id}\`)
    .then(response => response.json());
};
\`\`\`

### Python:
\`\`\`python
def fibonacci(n):
    if n <= 1:
        return n
    return fibonacci(n-1) + fibonacci(n-2)

result = [fibonacci(i) for i in range(10)]
print(result)
\`\`\`

### HTML/CSS:
\`\`\`html
<div class="container">
  <h1>Привет, мир!</h1>
  <p>Это пример HTML</p>
</div>
\`\`\`

### Bash команды:
\`\`\`bash
npm install react-markdown
npm run dev
git add .
git commit -m "Add markdown renderer"
\`\`\`

## Цитаты

> Это простая цитата. Она может содержать несколько строк текста.
>> Вложенная цитата
>>> Третий уровень вложенности

> **Важно:** Цитаты отлично подходят для выделения важной информации или цитирования источников.

## Ссылки

[Обычная ссылка](https://example.com)

[Ссылка с тайтлом](https://example.com "Тайтл ссылки")

Автоматические ссылки: <https://example.com>

Email ссылки: <example@example.com>

## Изображения

![Альтернативный текст](https://img.freepik.com/free-photo/view-beautiful-persian-domestic-cat_23-2151773947.jpg?semt=ais_hybrid "Всплывающая подсказка")

## Горизонтальная линия

Текст до линии

---

Текст после линии

***

Текст после еще одной линии

---

## Таблицы

### Простая таблица:
| Заголовок 1 | Заголовок 2 | Заголовок 3 |
|-------------|-------------|-------------|
| Ячейка 1    | Ячейка 2    | Ячейка 3    |
| Данные 4    | Данные 5    | Данные 6    |

### Таблица с выравниванием:
| Выравнивание влево | По центру | Выравнивание вправо |
|:------------------|:---------:|--------------------:|
| Левый текст       | Центр     | Правый текст        |
| Еще одна строка   | По центру | Еще один правый     |
| Значение          | Значение  | Значение            |

### Таблица с форматированием:
| Имя | Роль | Статус |
|-----|------|--------|
| **Иван** | Разработчик | ✅ Активен |
| *Мария* | Дизайнер | ❌ Не активен |
| \`Петр\` | Менеджер | ⏳ В ожидании |

## Комбинированное форматирование

### Список со ссылками и кодом:
- [React документация](https://react.dev)
- \`npm install react-markdown\` - установка пакета
- **Важно:** Не забывайте про типизацию
- Пример кода: \`const result = await fetchData()\`

### Таблица с разными элементами:
| Элемент | Пример | Описание |
|---------|--------|----------|
| Жирный | **Bold** | Используется для акцентов |
| Курсив | *Italic* | Для выделения |
| Код | \`console.log()\` | Для технических терминов |
| Ссылка | [Google](https://google.com) | Внешние ресурсы |

## Экранирование символов

Специальные символы: \\*не курсив\\* \\[не ссылка\\] \\#не заголовок

Обратные слэши для экранирования: \`\`\` (тройные кавычки)


## Сложные примеры

### Вложенные списки с форматированием:
1. **Первый уровень**
   - *Второй уровень* с \`кодом\`
     - [Ссылка](https://example.com) внутри списка
       1. Нумерованный подпункт
       2. Еще один подпункт
2. **Второй уровень**
   > Цитата внутри списка
   > 
   > \`\`\`js
   > // Код внутри цитаты в списке
   > console.log('Hello');
   > \`\`\`

### Комбинация всего и сразу:

> ## Заголовок внутри цитаты
> 
> Это **важное** сообщение с:
> - *Списком* элементов
> - \`Кодом\` внутри
> - [Ссылкой](https://example.com)
> 
> \`\`\`javascript
> // Код внутри цитаты
> const message = "Hello from quote!";
> console.log(message);
> \`\`\`
> 
> *— Автор цитаты*

---

## Заключение

Теперь вы видите, что компонент поддерживает:

- ✅ Все уровни заголовков (H1-H4)
- ✅ Жирный, курсив и комбинации
- ✅ Маркированные и нумерованные списки
- ✅ Вложенные списки
- ✅ Чеклисты (GFM)
- ✅ Блоки кода с подсветкой синтаксиса
- ✅ Инлайн код
- ✅ Цитаты с вложениями
- ✅ Ссылки и email
- ✅ Изображения
- ✅ Горизонтальные линии
- ✅ Таблицы с выравниванием
- ✅ Экранирование символов
- ✅ HTML элементы
- ✅ И многое другое!

Этот пример затрагивает **все** возможности вашего Markdown рендерера.`;

export const DocumentationPage = () => {
  const router = useRouter();

  return (
    <Layout isRenderSideBar={false} isCentered={false}>
      <div className="w-screen h-full relative">
        <Button
          className="fixed top-4 left-5"
          icon={FaArrowLeft}
          iconPosition="left"
          onClick={() => router.back()}
          variant="ghost"
        ></Button>
        <Document
          className="px-20"
          documentId="test"
          title="Documentation"
          content={markdown}
        />
      </div>
    </Layout>
  );
};
